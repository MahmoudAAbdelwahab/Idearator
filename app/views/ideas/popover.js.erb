$('a[data-popover-idea-id]').popover({
  animation: true,
  html: true,
  content:'<%= escape_javascript(render(:partial => "popover")) %>',
  container: 'body',
  placement: 'bottom'
});

$('a[data-popover-idea-id="<%=escape_javascript(params[:id])%>"]').popover('show')